import { TransformForexPipe } from './transform-forex.pipe';

describe('TransformForexPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformForexPipe();
    expect(pipe).toBeTruthy();
  });
});
