import { TransformMarginAccountsPipe } from './transform-margin-accounts.pipe';

describe('TransformMarginAccountsPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformMarginAccountsPipe();
    expect(pipe).toBeTruthy();
  });
});
