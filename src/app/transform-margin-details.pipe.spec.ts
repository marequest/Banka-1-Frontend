import { TransformMarginDetailsPipe } from './transform-margin-details.pipe';

describe('TransformMarginDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformMarginDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
