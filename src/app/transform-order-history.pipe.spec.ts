import { TransformOrderHistoryPipe } from './transform-order-history.pipe';

describe('TransformOrderHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformOrderHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
